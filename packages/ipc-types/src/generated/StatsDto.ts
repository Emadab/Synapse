// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { AnswerButtons } from "./AnswerButtons";
import type { DayCount } from "./DayCount";
import type { DeckStat } from "./DeckStat";
import type { FsrsStats } from "./FsrsStats";
import type { HourlyStat } from "./HourlyStat";
import type { RetentionWeek } from "./RetentionWeek";

/**
 * Aggregate collection statistics for the dashboards.
 */
export type StatsDto = { total_reviews: number, studied_days: number, 
/**
 * True-retention pass rate over the selected range, as a percentage (0–100).
 */
retention_pct: number, 
/**
 * Target retention for the reference line: the filtered deck's FSRS
 * `desired_retention` when set, else the default (90%). 0–100.
 */
retention_goal_pct: number, total_time_ms: number, new_count: number, learning_count: number, young_count: number, mature_count: number, suspended_count: number, 
/**
 * Reviews per collection-relative day (all time), ascending.
 */
reviews: Array<DayCount>, 
/**
 * Due review cards per day-offset (0..=30 from today).
 */
forecast: Array<DayCount>, 
/**
 * Cards already overdue (due < today).
 */
backlog_count: number, 
/**
 * Anchor for mapping day indices in `reviews`/`forecast` to calendar dates:
 * collection-day `d` started at `day0_ms + d * 86_400_000`.
 */
day0_ms: number, retention_weekly: Array<RetentionWeek>, answer_buttons: AnswerButtons, 
/**
 * Reviews by hour-of-day, shifted to the caller's local timezone.
 */
hourly: Array<HourlyStat>, fsrs: FsrsStats, 
/**
 * Always all-decks, regardless of the `deck_id` filter; powers the deck table.
 */
deck_stats: Array<DeckStat>, };
